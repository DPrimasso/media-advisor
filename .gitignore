# Dependencies & build
node_modules/
dist/
build/
.venv/
.python-version
*.egg-info/

# Environment
.env
.env.local

# Generated data copied into web public (always regenerated)
web/public/analysis/

# Data folders (tracked to sync work across machines)
# - data/transcripts/  : raw transcripts + metadata
# - data/analysis/     : per-video analysis outputs
# - mercato/      : mercato index + tips + transfers + caches
# - channels/*.json: channel registry + per-channel video lists + dates cache

# IDE
.vscode/
*.swp
*.swo
.claude/settings.local.json

# Python caches
__pycache__/
.mypy_cache/
.pytest_cache/
.ruff_cache/
